CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_INPUT=y

# Removed: CONFIG_ZMK_BLE_EXPERIMENTAL_CONN — unmet dep (ZMK_BLE=n) on dongle
# Removed: CONFIG_ZMK_RGB_UNDERGLOW — no zmk,underglow node on dongle; moved to charybdis_left.conf
# Removed: CONFIG_WS2812_STRIP — auto-enabled by Zephyr 4.1
# Removed: central battery options — require ZMK_SPLIT_ROLE_CENTRAL; moved to charybdis_dongle.conf
